import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    wrapper: {
        width: 70,
    },
    bg: {
        background: 'red',
    },
    color:{
        color: 'pink',
    }
});
