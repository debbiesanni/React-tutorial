import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    holder: {
        height: '300px',
        width: '300px',
    },
    iconFont:{
        fontSize: 100
    }
});
