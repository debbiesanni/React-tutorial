import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    holder: {
        height: '10rem',
        width: '20rem',
    },
    iconFont:{
        fontSize: 100
    }
});
